# Rust Project Scaffolder Pipeline — operator guide

> Horde generated by **Rookery**. Pipeline: `ingest → structure → investigate → mock-builder → todo-generator → deliver`.

## Validate

```bash
cargo run -p kowalski-cli -- agent-app validate --path .
```

## Layout

- `horde.md` — manifest
- `agents/*.md` — one penguin per pipeline step
- `prompts/*.md` — LLM prompts
- `workdir` — runtime output (see `horde.md`)
