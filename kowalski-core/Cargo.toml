[package]
name = "kowalski-core"
version = "1.6.0"
edition = "2024"
description = "Kowalski Core Module: A Rust-based agent for interacting with Ollama models"
license = "MIT"
authors = ["yarenty <yarenty@gmail.com>"]
repository = "https://github.com/yarenty/kowalski"
homepage = "https://github.com/yarenty/kowalski"
readme = "README.md"
categories = [
    "network-programming",
    "text-processing",
    "science",
]
keywords = [
    "AI",
    "agent",
    "chat",
    "llm",
    "ollama",
]

[features]
default = []
## PostgreSQL: `sqlx` Postgres driver, **`pgvector`** (SQLx bindings), episodic/semantic SQL + migrations under `migrations/postgres/`.
postgres = [
    "sqlx/postgres",
    "dep:pgvector",
    "pgvector/sqlx",
]

[dependencies]
async-trait = {workspace = true}
tokio-util = {workspace = true}
futures = {workspace = true}
serde = { workspace= true,features = ["derive"] }
serde_json = {workspace = true}
reqwest = { workspace = true, features = ["blocking"] }
tokio = {workspace = true}
thiserror = {workspace = true}
uuid = { version = "1.7", features = ["v4", "serde"] }
chrono = { version = "0.4", features = ["serde"] }
log = {workspace = true}

config = { workspace = true }
dirs = { workspace = true }
toml = { workspace = true }
env_logger = { workspace = true }
url = "2.5"

# Dependencies from kowalski-memory
rand = { workspace = true }
once_cell = "1.18"

# Dependencies from kowalski-agent-template
bytes = "1.5"
tower = "0.5"
tower-http = { workspace = true, features = ["trace"] }
regex = "1.10"
markdown = "1.0"
llm_json = "1.0.2"
async-openai = { version = "0.41", features = ["native-tls", "chat-completion", "embedding"] }
async-stream = "0.3"
sqlx = { version = "0.9", default-features = false, features = ["runtime-tokio", "sqlite", "migrate", "macros", "tls-native-tls", "chrono"] }
pgvector = { version = "0.4", optional = true, default-features = false }

[dev-dependencies]
tempfile = "3.25.0"
axum = { workspace = true }
